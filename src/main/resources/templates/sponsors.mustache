{{> header}}

<div class="mxt-wrapper--space"></div>

<div class="row">
  <div class="column large-8 large-offset-2">

    <h1 class="text-center">{{#i18n}}sponsors.title{{/i18n}}</h1>
    <p class="text-center">{{#i18n}}sponsors.text{{/i18n}}</p>
    <hr />

    <h2 class="subheader text-center mxt-subheader--space">{{#i18n}}sponsors.gold{{/i18n}}</h2>

    <div class="mxt-wrapper--sponsors callout">
        {{#sponsors-gold}}
        <div class="row align-middle">
            <div class="small-12 medium-4 columns text-center">
                <img src="/images/{{sponsor.logoUrl}}" class="mxt-img--sponsors" id="{{#urlEncode}}{{sponsor.login}}{{/urlEncode}}"/>
            </div>
            <div class="small-12 medium-8 columns">
                <h3>NOM DU SPONSOR</h3>
                {{sponsor.description}}
                <ul class="menu simple">
                    {{#sponsor.links}}
                        <li><a href="{{url}}" target="_blank">{{name}}</a></li>
                    {{/sponsor.links}}
                </ul>
            </div>           
        </div>
        {{/sponsors-gold}}
    </div>

    <h2 class="subheader text-center mxt-subheader--space">{{#i18n}}sponsors.silver{{/i18n}}</h2>

    <div class="mxt-wrapper--sponsors callout">
        {{#sponsors-silver}}
        <div class="row align-middle">
            <div class="small-12 medium-4 columns text-center">
                <img src="/images/{{sponsor.logoUrl}}" class="mxt-img--sponsors" id="{{#urlEncode}}{{sponsor.login}}{{/urlEncode}}"/>
            </div>
            <div class="small-12 medium-8 columns">
                <h3>NOM DU SPONSOR</h3>
                {{sponsor.description}}
                <ul class="menu simple">
                    {{#sponsor.links}}
                        <li><a href="{{url}}" target="_blank">{{name}}</a></li>
                    {{/sponsor.links}}
                </ul>
            </div>
        </div>
        {{/sponsors-silver}}
    </div>

    <h2 class="subheader text-center mxt-subheader--space">{{#i18n}}sponsors.party{{/i18n}}</h2>

    <div class="mxt-wrapper--sponsors callout">
        {{#sponsors-party}}
        <div class="row align-middle">
            <div class="small-12 medium-4 columns text-center">
                <img src="/images/{{sponsor.logoUrl}}" class="mxt-img--sponsors" id="{{#urlEncode}}{{sponsor.login}}{{/urlEncode}}"/>
            </div>
            <div class="small-12 medium-8 columns">
                <h3>NOM DU SPONSOR</h3>
                {{sponsor.description}}
                <ul class="menu simple">
                    {{#sponsor.links}}
                        <li><a href="{{url}}" target="_blank">{{name}}</a></li>
                    {{/sponsor.links}}
                </ul>
            </div>
        </div>
        {{/sponsors-party}}
    </div>

    <h2 class="subheader text-center mxt-subheader--space">{{#i18n}}sponsors.lanyard{{/i18n}}</h2>

    <div class="mxt-wrapper--sponsors callout">
        {{#sponsors-lanyard}}
        <div class="row align-middle">
            <div class="small-12 medium-4 columns text-center">
                <img src="/images/{{sponsor.logoUrl}}" class="mxt-img--sponsors" id="{{#urlEncode}}{{sponsor.login}}{{/urlEncode}}"/>
            </div>
            <div class="small-12 medium-8 columns">
                <h3>NOM DU SPONSOR</h3>
                {{sponsor.description}}
                <ul class="menu simple">
                    {{#sponsor.links}}
                        <li><a href="{{url}}" target="_blank">{{name}}</a></li>
                    {{/sponsor.links}}
                </ul>
            </div>
        </div>
        {{/sponsors-lanyard}}
    </div>

    <h2 class="subheader text-center mxt-subheader--space">{{#i18n}}sponsors.hosting{{/i18n}}</h2>

    <div class="mxt-wrapper--sponsors callout">
        {{#sponsors-hosting}}
        <div class="row align-middle">
            <div class="small-12 medium-4 columns text-center">
                <img src="/images/{{sponsor.logoUrl}}" class="mxt-img--sponsors" id="{{#urlEncode}}{{sponsor.login}}{{/urlEncode}}" />
            </div>
            <div class="small-12 medium-8 columns">
                <h3>NOM DU SPONSOR</h3>
                {{sponsor.description}}
                <ul class="menu simple">
                    {{#sponsor.links}}
                        <li><a href="{{url}}" target="_blank">{{name}}</a></li>
                    {{/sponsor.links}}
                </ul>
            </div>
        </div>
        {{/sponsors-hosting}}
    </div>

  </div>
</div>

{{> footer}}
